# np_assignment1

To compile
~~~shell
make

# then all the binary files are generated in ./bin/
./server 0.0.0.0:<port>
./client 127.0.0.1:<port>
~~~


To clean the object files

~~~shell
make clean
~~~
