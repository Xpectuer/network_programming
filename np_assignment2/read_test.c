#include<stdio.h>
#include<stdlib.h>
#include<unistd.h>
#include "read.h"

void init()
{
    setbuf(stdin,0);
    setbuf(stdout,0);
    setbuf(stderr,0);
}

int main(int argc, char** argv)
{
	char* str = "123";
	
	

    return EXIT_SUCCESS;
}
